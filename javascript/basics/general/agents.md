# Agents Instructions

## Repository context
JavaScript-only learning platform.  
Keep all contributions in vanilla JavaScript (no TypeScript) and avoid adding build tooling beyond `pnpm` scripts and Jest.

## Expectations for agents
- When adding features, include matching Jest tests under `tests/` and update the README/practice exercise list when new concepts are introduced.

## Suggested future tasks


